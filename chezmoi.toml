# chezmoi configuration
# The source directory is the repo root (where this file lives).
# Managed files are in the "dotfiles/" subdirectory.

[edit]
  command = "code"

[add]
  templateSymlinks = true

[diff]
  pager = "less -R"